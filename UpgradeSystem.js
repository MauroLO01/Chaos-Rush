export default class UpgradeSystem {
  constructor(scene) {
    this.scene = scene;
  }

  open(player) {
    console.log('🧙‍♂️ Escolha uma melhoria!');
    // futuro: abrir UI com opções (aura maior, dano, velocidade, etc)
  }
}
